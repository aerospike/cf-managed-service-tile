meta:
  environment: aerospikeserver-warden

  stemcell:
    name: bosh-warden-boshlite-ubuntu-trusty-go_agent
    version: latest

update:
  canaries: 1
  max_in_flight: 50
  canary_watch_time: 1000-900000
  update_watch_time: 1000-900000

compilation:
  cloud_properties:
    name: random

resource_pools:
- name: vms
  network: default
  cloud_properties:
    instance_type: default
- name: aerospike-nodes
  network: aerospike-nodes
  cloud_properties:
    instance_type: default

networks:
- name: default
  type: dynamic
- name: aerospike-nodes
  type: dynamic