# abort script on any command that exits with a non zero value
set -e

cp aerospike_service_broker/service-broker-0.1.3.jar ${BOSH_INSTALL_TARGET}
cp aerospike_service_broker/manifest.yml ${BOSH_INSTALL_TARGET}
